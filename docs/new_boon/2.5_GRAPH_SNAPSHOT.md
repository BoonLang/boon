## Chapter 2.5: Graph Snapshot

### 2.5.1 Serializable State

All runtime state must be serializable.

**See Issue 9 for canonical GraphSnapshot definition** (includes pending_messages, pending_dom_events, transport_state).

**See Issue 24 for canonical NodeSnapshot definition** (includes subscribers, kind-specific state via NodeStateSnapshot).

### 2.5.2 Snapshot/Restore API

```rust
impl EventLoop {
    pub fn snapshot(&self) -> GraphSnapshot { ... }
    pub fn restore(&mut self, snapshot: GraphSnapshot) { ... }
}
```

**Files to modify:**
- `crates/boon/src/platform/browser/engine.rs` - Snapshot types and methods

---

