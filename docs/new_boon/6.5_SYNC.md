## Documentation Sync Needed

After plan approval, the following changes must be synced across docs/new_boon/:

### 1.1_GETTING_STARTED.md Updates Required:
1. **Add TextTemplate node** to node types table
2. **Success criteria**: Change "No Arcs" to "No Arc<ValueActor>"
3. **Add sections for:** Alias field-paths (Issue 14), Effect nodes (Issue 18), Tick quiescence (Issue 20)

### 2.1_NODE_IDENTIFICATION.md Updates Required:
1. **ScopeId propagation rules**: Remove HOLD iteration references (already fixed per Issue 1)

### 2.3_MESSAGE_PASSING.md Updates Required:
1. **Payload definition**: Ensure single canonical definition per Issue 13
2. **Add BLOCK compilation section** per Issue 19

### 2.4_EVENT_LOOP.md Updates Required:
1. **Timer implementation**: Add wall-clock integration per Issue 6
2. **Tick quiescence**: Add full algorithm per Issue 20

### 2.5_GRAPH_SNAPSHOT.md Updates Required:
1. Expand per Issue 9 and add NodeSnapshot definition per Issue 24

### 2.8_BRIDGE_API.md Updates Required:
1. **LINK binding protocol** per Issue 15

### 6.2_EXAMPLES.md Updates Required:
1. **Fix line 508**: Change `item_index` to `item_key` (critical bug)
2. **Add analysis** for 10 missing examples (list_retain_count, list_object_state, etc.)
3. **Mark outdated** the 4 examples not in playground (latest, then, when, while)
4. **Update header** to say "23 examples" not "all playground examples"

---

