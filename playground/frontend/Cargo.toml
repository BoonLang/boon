[package]
name = "frontend"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
boon = { path = "../../crates/boon" }
ulid = { version = "1.2.0", features = ["serde"] }

[features]
default = ["engine-v2"]
# Use the new arena-based engine (engine_v2)
engine-v2 = ["boon/engine-v2"]
